- name: single item tuple
  type: no_change
  in_block: true
  formatted: |
    const X: (Type,);
- name: Breakpoint with semicolon
  type: breakpoint
  in_block: true
  before: |
    const X: (Type,);
  after: |
    const X: (
        Type,
    );
